# Core API
fastapi==0.132.0
uvicorn[standard]==0.41.0
pydantic==2.12.5
pydantic-settings==2.9.1
python-multipart==0.0.22
python-dotenv==1.2.1
starlette==0.52.1

# Database
sqlalchemy==2.0.46
alembic==1.18.4
psycopg2-binary==2.9.10

# Security / Auth
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2  # passlib 1.7.4 detect_wrap_bug is incompatible with bcrypt ≥4
cryptography==46.0.5

# File Processing
PyMuPDF==1.27.1
pdfplumber==0.11.6
python-docx==1.1.2

# NLP / AI
spacy==3.8.5
# CPU-only torch (avoids large CUDA download in Docker)
torch==2.7.0+cpu ; sys_platform != "win32"
torch==2.7.0 ; sys_platform == "win32"
sentence-transformers==4.1.0
numpy==2.4.2

# HTTP Client
httpx==0.28.1
requests==2.32.5

# HTML Parsing (Job Description URL fetching)
beautifulsoup4==4.12.3

# PDF report generation (Career Report Export)
reportlab==4.2.5

# Rate Limiting
slowapi==0.1.9

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
