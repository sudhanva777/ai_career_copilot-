# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# Must be >= 32 characters or the server will refuse to start
SECRET_KEY=replace-this-with-a-64-char-random-hex-string-generated-by-secrets

# ── Backend ───────────────────────────────────────────────
DATABASE_URL=postgresql://postgres:postgres@db:5432/aicopilot
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=aicopilot
OLLAMA_BASE_URL=http://ollama:11434
UPLOAD_DIR=./uploads
ALLOWED_ORIGINS=http://localhost:3000
VITE_API_BASE=http://localhost:8000/api/v1
